#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn
  (ros:ensure-asdf))

(defpackage :ros.script.mkfile.3728583506 (:use :cl))
(in-package :ros.script.mkfile.3728583506)

(load "main.lisp")

(defun main (&rest argv)
  (declare (ignorable argv))
  (-main- argv))
